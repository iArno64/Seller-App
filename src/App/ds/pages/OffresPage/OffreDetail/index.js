//import { useLoaderData, useRouteLoaderData, useRoutes } from "react-router-dom";

export default function Offres() {
  // const mesRoutes = useRoutes();
  //const post = useLoaderData();
  //const r = useRouteLoaderData();

  return (
    <>
      <div>
        Detail offres
        <div>
          {/*  mesRoutes : {mesRoutes}
          // mesPosts : {post}
          //mesRoutesLoader : {r} */}
          {/* {post.body.split("\n").map((paragraph, index) => (
            <p key={`paragraph-${index + 1}`}>{paragraph}</p>
          ))} */}
        </div>
      </div>
    </>
  );
}
